/**
 * Public surface of the legal feature.
 *
 * Add new legal-related exports here so the rest of the app imports from
 * `@/features/legal` rather than reaching into individual files.
 */
export { PrivacyConsentGate } from './PrivacyConsentGate';
